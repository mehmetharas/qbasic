CLS
RANDOMIZE TIMER
DO
X = INT(RND * 15): IF X = 0 THEN X = 1
Y = INT(RND * 75): IF Y = 0 THEN Y = 1
1 R = INT(RND * 15): IF R = 0 THEN R = 1
COLOR R
FOR H = 1 TO 1000000: NEXT H
LOCATE X, Y: PRINT "*"

key$ = INKEY$

LOOP UNTIL INKEY$ = CHR$(27)

